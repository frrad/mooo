// Package buildinfo exposes build metadata without coupling protocol packages to
// a command or release system.
package buildinfo

// Version is replaced at release build time.
var Version = "devel"
